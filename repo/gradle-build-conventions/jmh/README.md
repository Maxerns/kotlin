# JMH (Java Microbenchmark Harness)

This convention applies the `me.champeau.jmh` plugin and propagates the JVM toolchain configured via:
```
jmh {
    javaLauncher = ...
}
```
to all the JMH tasks.
